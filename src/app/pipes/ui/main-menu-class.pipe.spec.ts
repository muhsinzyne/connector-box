import { MainMenuClassPipe } from './main-menu-class.pipe';

describe('MainMenuClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MainMenuClassPipe();
    expect(pipe).toBeTruthy();
  });
});
