import { MainMenuHasChildrenPipe } from './main-menu-has-children.pipe';

describe('MainMenuHasChildrenPipe', () => {
  it('create an instance', () => {
    const pipe = new MainMenuHasChildrenPipe();
    expect(pipe).toBeTruthy();
  });
});
